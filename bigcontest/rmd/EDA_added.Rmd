---
title: "EDA_added"
output: html_document
date: "2022-10-13"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r, message=FALSE, warning=FALSE}
library(dplyr)
options(dplyr.summarise.inform = FALSE) 
library(tidyr)
library(lubridate)
library(ggplot2)
library(lubridate)
library(plotly)
```

```{r}
data <- read.csv("../../../../../bigcontest/data/preprocessed/data.csv")
colnames(data)
```

```{r}
data[1:100,]
```
```{r}
data$is_applied <- factor(data$is_applied)
```
```{r}
ggplot(data = data, mapping = aes(x = is_applied,  y = log10(loan_limit), color = is_applied)) +
  geom_boxplot() +
  theme_minimal() +
  coord_flip()
```

